server:
  port: ${PORT:8082}

spring:
  application:
    name: doctor-service

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/doctors_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:076493}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://${EUREKA_USERNAME:eureka}:${EUREKA_PASSWORD:password}@localhost:8761/eureka/}
    registry-fetch-interval-seconds: 5
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 30

logging:
  level:
    com.healthcare.doctor: DEBUG

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:dlpanigdn}
  api-key: ${CLOUDINARY_API_KEY:285439836477588}
  api-secret: ${CLOUDINARY_API_SECRET:Ze6nQHWbBbQIChe0YtQ7XvyU3Uw}